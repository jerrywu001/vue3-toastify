import { defineConfig } from 'windicss/helpers';

export default defineConfig({
  darkMode: 'class',
  safelist: 'py-3 py-4 py-5',
  theme: {
    extend: {
      colors: {
      },
    },
  },
  plugins: [],
});
